{
  "name": "@openlife-au/workspace",
  "private": true,
  "version": "0.0.0",
  "description": "Open Life Data Standards (AU) \u2014 vendor-neutral contracts for Australian life insurance",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/axiscrm/open-life-au.git"
  },
  "engines": {
    "node": ">=20"
  },
  "type": "module",
  "scripts": {
    "lint": "redocly lint",
    "bundle": "npm run bundle:quoting && npm run bundle:policy && npm run bundle:requirements && npm run bundle:commissions",
    "bundle:quoting": "redocly bundle domains/quoting/openapi.yaml -o dist/quoting/openapi.yaml && redocly bundle domains/quoting/openapi.yaml -o dist/quoting/openapi.json",
    "docs": "node scripts/build-site.mjs",
    "postman": "node scripts/build-postman.mjs",
    "mock": "node scripts/mock.mjs",
    "examples": "node scripts/validate-examples.mjs",
    "audit": "node scripts/npm-audit-gate.mjs --level moderate",
    "provenance": "node scripts/check-provenance.mjs",
    "verify": "npm run lint && npm run versions && npm run dist:check && npm run examples && npm run rejections && npm run postman:check && npm run provenance",
    "rejections": "node scripts/check-rejections.mjs",
    "codegen:typescript": "bash scripts/check-codegen.sh typescript",
    "codegen:python": "bash scripts/check-codegen.sh python",
    "codegen:java": "bash scripts/check-codegen.sh java",
    "codegen:ruby": "bash scripts/check-codegen.sh ruby",
    "codegen:go": "bash scripts/check-codegen.sh go",
    "codegen:csharp": "bash scripts/check-codegen.sh csharp",
    "codegen": "for l in typescript python java ruby go csharp; do npm run codegen:$l || exit 1; done",
    "bundle:policy": "redocly bundle domains/policy/openapi.yaml -o dist/policy/openapi.yaml && redocly bundle domains/policy/openapi.yaml -o dist/policy/openapi.json",
    "postman:check": "npm run postman && git diff --exit-code -- postman/ || (echo '' && echo '\u2717 postman/ was stale. It has just been regenerated above \u2014 review and commit it.' && exit 1)",
    "dist:check": "npm run bundle && git diff --exit-code -- dist/ || (echo '' && echo '\u2717 dist/ was stale. It has just been rebuilt above \u2014 review and commit it.' && exit 1)",
    "bundle:requirements": "redocly bundle domains/requirements/openapi.yaml -o dist/requirements/openapi.yaml && redocly bundle domains/requirements/openapi.yaml -o dist/requirements/openapi.json",
    "mock:quoting": "node scripts/mock.mjs quoting",
    "mock:policy": "node scripts/mock.mjs policy",
    "mock:requirements": "node scripts/mock.mjs requirements",
    "mock:check": "node scripts/check-mock.mjs",
    "bundle:commissions": "redocly bundle domains/commissions/openapi.yaml -o dist/commissions/openapi.yaml && redocly bundle domains/commissions/openapi.yaml -o dist/commissions/openapi.json",
    "mock:commissions": "node scripts/mock.mjs commissions",
    "versions": "node scripts/check-versions.mjs"
  },
  "devDependencies": {
    "@hey-api/openapi-ts": "^0.99.0",
    "@redocly/cli": "^2.6.0",
    "@stoplight/prism-cli": "^5.14.2",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "typescript": "^5.9.3",
    "yaml": "^2.8.1"
  },
  "overrides": {
    "@hey-api/openapi-ts": {
      "js-yaml": "^4.3.0"
    }
  },
  "scarfSettings": {
    "enabled": false
  }
}
